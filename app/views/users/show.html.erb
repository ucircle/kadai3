<div>
  <%= flash[:notice] %>
</div>

<div class="container">
  <div class="row">
    <div class="col-4">
<%= render 'books/list', books: @books, new_book: @new_book, user: @user %>
    </div>


<div class="col-8">
<h1>Books</h1>
 <table class="table">
  <thead>
    <tr>
      <th>Title</th>
      <th>opinion</th>
      <th></th>
    </tr>

  </thead>

  <tbody>

  <% @user.books.each do |book| %>

   <tr>
     <td><%= book.title %></td>
     <td><%= book.body %></td>

   </tr>
  <% end %>


  </tbody>

  </table>
  </div>
  </div>
</div>