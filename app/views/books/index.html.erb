<%= render 'books/list', books: @books, new_book: @new_book, user: @user %>



<table>

<h1>投稿一覧</h1>

<%= @books.each do |book| %>

  <p>title</p>
  <span><%= link_to book.title, "/books/#{book.id}" %></span>
  <p>opinion</p>
  <span><%= book.body %></span>
  
  
<% end %>

</table>