<%= render 'books/list', new_book: @new_book, user: @user %>

<h1>Books</h1>
 <table>
  <thead>
    <tr>
      <th>Title</th>
      <th>opinion</th>
    </tr>

  </thead>

<tbody>

   <tr>
     <p>投稿ユーザー画像：<%= image_tag @user.get_profile_image(100,100) %></p>
     <td><%= @book.title %></td>
     <td><%= @book.body %></td>
   </tr>


</tbody>

</table>
