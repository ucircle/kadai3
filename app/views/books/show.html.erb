<div>
  <%= flash[:notice] %>
</div>

<div class="container">
  <div class="row">
    <div class="col-4">
<%= render 'books/list', new_book: @new_book, user: @user %>
    </div>
<div class="col-8">
<table class="table">
<thead>
  <h1>Book detail</h1>
</thead>

<tbody>

   <tr>
     <td><%= link_to image_tag @user.get_profile_image(100,100) %><br>
     <%= @user.name %></td>
     <td><%= link_to @book.title, book_path(@book.id) %></td>
     <td><%= @book.body %></td>

     <% if @book.user == current_user %>

       <td><%= link_to "edit", edit_book_path(@book.id), class:"btn btn-success" %></td>
       <td><%= link_to "destroy", book_path(@book.id), class:"btn btn-danger", method: :delete, "data-confirm" => "本当に削除しますか？" %></td>
     <% end %>
   </tr>


  </tbody>

  </table>
  </div>
  </div>
</div>